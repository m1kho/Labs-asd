using System;

namespace lvl2
{
    public static class CombinatoricsMath
    {
        public static double Power(double baseVal, int exp)
        {
            return Math.Pow(baseVal, exp);
        }
    }
}